//defining all the attributes that a student is gonna has
//this represents the type of data wherever a call send to the backend
export interface Student {

    id: number;
    firstName: string;
    lastName: string;
    phone: string;
    course: string;
    imageUrl: string;
    studentCode: string;

}
